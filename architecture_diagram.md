# Deckzi AI Chat - Architecture Diagram

```mermaid
graph TD
    %% Define User
    User[User / Client]

    %% Define Frontend
    subgraph Frontend [Angular Frontend - Port 4200]
        App[Deckzi UI]
        AuthSvc[Auth Service]
        ChatSvc[Chat Service]
        JWTInterceptor[JWT Interceptor]
        
        App --> AuthSvc
        App --> ChatSvc
        ChatSvc --> JWTInterceptor
    end

    %% Define Backends
    subgraph FastAPI_Backend [FastAPI AI Backend - Port 8000]
        ChatRouter[Chat Router]
        HistorySvc[History Service]
        GeminiWrapper[Gemini API Integration]
        F_Auth[JWT Auth Handler]
        
        ChatRouter --> F_Auth
        ChatRouter --> GeminiWrapper
        ChatRouter --> HistorySvc
    end

    subgraph DotNet_Backend [.NET Core Backend - Port 5000]
        UserCtrl[User Controller]
        ProfileCtrl[Profile Controller]
        D_Auth[JWT Service]
        MySqlContext[Entity Framework Core]
        
        UserCtrl --> D_Auth
        ProfileCtrl --> D_Auth
        UserCtrl --> MySqlContext
        ProfileCtrl --> MySqlContext
    end

    %% External Services
    Gemini[Google Gemini 1.5 Flash]

    %% Databases
    subgraph Databases
        MySQL[(MySQL - Profiles)]
        MongoDB[(MongoDB - Chat History)]
    end

    %% Connections
    User --> App

    %% API Calls
    AuthSvc -- "Login/Register" --> UserCtrl
    ChatSvc -- "Stream Chat" --> ChatRouter
    App -- "Get Profile" --> ProfileCtrl

    %% Backend to External/DB
    GeminiWrapper -- "Streaming Request" --> Gemini
    HistorySvc --> MongoDB
    MySqlContext --> MySQL

    %% Shared Auth Note
    F_Auth -. "Validates Same Secret" .-> D_Auth
```

## System Components

### 1. Angular Frontend
- **Deckzi UI**: Built with Angular 18 using Reactive Forms and a glassmorphism dark theme.
- **Chat Service**: Handles real-time streaming using the `Fetch API` and `ReadableStream`.
- **JWT Interceptor**: Automatically attaches the authorization token to outgoing requests.

### 2. FastAPI Backend (AI Service)
- **AI Integration**: Connects to **Gemini 1.5 Flash** for token-by-token streaming responses.
- **Prompt Protection**: Implements regex-based prompt injection detection.
- **Persistence**: Stores session-based conversation history in **MongoDB**.

### 3. .NET Core Backend (User Service)
- **User Management**: Handles registration and login using BCrypt for password hashing.
- **Profile Service**: Manages user preferences and display settings.
- **Persistence**: Uses **Entity Framework Core** to manage data in **MySQL**.

### 4. Shared Security
- **JWT Authentication**: Both backends share a common JWT secret, allowing a single token generated by either service to be validated by the other.
